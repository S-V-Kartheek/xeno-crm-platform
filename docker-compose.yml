services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: smartcrm
      POSTGRES_USER: smartcrm
      POSTGRES_PASSWORD: smartcrm
    ports:
      - "5432:5432"
    volumes:
      - smartcrm_pg:/var/lib/postgresql/data

volumes:
  smartcrm_pg:
